@model SistemaTaskWhatsapp.Models.ViewModels.TareaVM

@{
    ViewData["Title"] = "Editar tarea";
}

<style>
    /* Animación general de la vista */
    .fade-in-page {
        animation: fadeIn 0.6s ease forwards;
    }

    @@keyframes fadeIn {
        from {
            opacity: 0;
            transform: translateY(15px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

</style>

<div class="contenedor-principal">
    <div class="text-center">
        <h3 class="col-3">@ViewData["Title"]</h3>
    </div>
    <div class="col-5 mt-4 border rounded shadow p-3 fade-in-page">
        <form asp-action="Edit" method="post">
            <div asp-validation-summary="All" class="text-danger"></div>
            <input type="hidden" asp-for="Tarea.Id"/>
            <input type="hidden" asp-for="Tarea.FechaInicio" />
            <div class="form-group">
                <label for="nombre">Nombre:</label>
                <input type="text" id="nombre" asp-for="Tarea.Nombre" class="form-control" placeholder="Escriba el nombre del proyecto..." />
                <span asp-validation-for="Tarea.Nombre" class="text-danger"></span>
            </div>
            <br />
            <div class="form-group">
                <label for="descripcion">Descripción:</label>
                <textarea type="text" id="descripcion" asp-for="Tarea.Descripcion" class="form-control" placeholder="Escriba una descripción acerca del proyecto..."></textarea>
                <span asp-validation-for="Tarea.Descripcion" class="text-danger"></span>
            </div>
            <br />
            <div class="form-group">
                <label for="empresa">Proyecto:</label>
                <select id="empresa" asp-for="Tarea.ProyectoId" asp-items="@Model.ListaProyectos" class="form-select">
                    <option disabled selected value="">--Elija un proyecto--</option>
                </select>
                <span asp-validation-for="Tarea.ProyectoId" class="text-danger"></span>
            </div>
            <br />
            <div class="form-group">
                <label for="estado">Estado:</label>
                <select id="estado" asp-for="Tarea.Estado" class="form-select">
                    <option disabled selected value="">--Elija un estado--</option>
                    <option value="0">Pendiente</option>
                    <option value="1">Finalizada</option>
                </select>
                <span asp-validation-for="Tarea.Estado" class="text-danger"></span>
            </div>
            <br />
            <div class="form-group">
                <label for="fechaentrega">Fecha de entrega:</label>
                <input id="fechaEntrega" type="date" asp-for="Tarea.FechaEntrega" class="form-control"/>
                <span asp-validation-for="Tarea.FechaEntrega" class="text-danger"></span>
            </div>
            <br />
            <div class="d-flex justify-content-center gap-2 mt-3">
                <a asp-action="Index" class="btn btn-secondary">Volver</a>
                <button type="submit" class="btn btn-success">Editar</button>
            </div>
        </form>
    </div>
</div>
